const { createTheme } = require("@mui/material");

const theme = createTheme({
  palette: {
    textColor: {
      primary: "#002C15",
      secondary: "#C3F53D",
      tertiary: "#FFFFFF",
      quaternary: "#000000",
    },
    primary: {
      main: "#002C15",
    },
    secondary: {
      main: "#C3F53D",
    },
  },
});
