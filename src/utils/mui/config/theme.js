const { createTheme } = require("@mui/material");

const theme = createTheme({
  palette: {
    textColor: {
      primary: "#002C15",
      secondary: "#C3F53D",
      light: "#FFFFFF",
      dark: "#000000",
    },
    primary: {
      main: "#002C15",
    },
    secondary: {
      main: "#C3F53D",
    },
    light: {
      main: "#FFFFFF",
    },
    dark: {
      main: "#000000",
    },
  },
});
